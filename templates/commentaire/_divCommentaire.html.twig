<div class="line-commentaire">
    <div class="auteur-commentaire">
        <p>{{commentaire.auteur.nom}} {{commentaire.auteur.prenom}}</p>
        <p>{{commentaire.dateCreation |date("m/d/Y") }} </p>
    </div>
    <p>{{commentaire.contenu}}</p>
</div>