<div class="comm-space">
    <h2>Ajouter un commentaire</h2>
    {{form_start(form, { 
        'attr': { novalidate: 'novalidate' },
        'action': path('ajouter_commentaire', {'id': article.id}),
        'method': 'POST',
    }) }}
        {{ form_widget(form.contenu) }}
        {{ form_widget(form.Ajouter) }}
    {{ form_end(form) }}
</div>